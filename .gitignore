# Go build artifacts
/sirenhead-tool
*.test
*.out

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Local project state — the CLI generates these when run; users
# do NOT want them checked into a fresh clone.
*.meta.json
text/units/*.json

# Temp scratch dirs from tests
/tmp/
